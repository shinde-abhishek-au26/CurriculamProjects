<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather App</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <header>
            <div class="compass-icon">
                <img src="/icons/compass.svg" alt="">
            </div>
        </header>
        <div class="weather-title">
            <h1>-: Simply Weather :-</h1>
            <p>View the Current Weather For Your City</p>
        </div>
        <form action="/" method="POST">
            <input type="text" name="city" class="ghost-input" placeholder="Enter Your City..">
        </form>
<!-- here we can use ejs syntax and  can add if conditions and send our data varibles here to render the orginal values from our api ! -->
        <div class="result">
            <% if(temp !== null) {%>
                <h1 class="temp"><%= temp %></h1>

            <% } %>    

            <div class="icons-des">
                <% if(icon!== null) {%>
                    <img class = "icon" src="https://openweathermap.org/img/w/<%= icon %>.png" alt="">
                <% } %> 

                <% if(des!== null) {%>
                    <h3><%= des %></h3>
                <% } %>  
            </div>
            <% if(city!== null) {%>
                <p class="city"><%= city %></p>
            <% } %> 
        </div>
    </div>
</body>
</html>